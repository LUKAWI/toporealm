id: e_gateway_module
source: l1_gateway_ui
target: l3_module_projection
type: depends_on
contract:
  produces: 模块状态、声明投影与动作执行边界
  consumed_by:
    - artifact: module-ui-host
      used_as: 解析贡献和动作可用性
  validation:
    criteria: 缺失模块保留原始数据并返回不可用原因
