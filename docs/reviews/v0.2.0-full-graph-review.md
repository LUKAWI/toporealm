# TopoRealm 0.2.0 独立全图复核

日期：2026-09-15
复核者：独立只读 agent `/root/full_graph_review`
状态：协调者已裁决，阻断项清零

## 规格轴

首轮复核发现四项阻断：validator 不可用时未 fail-closed、Workflow 0.1 relation 简写误判且兼容 notice 丢失、MCP/Server 切图未按目标图重装 runtime 且 Web 完整性误报、空图初始化绕过统一持久化边界。

协调者接受全部问题并完成修复。复查确认：

- 已声明 validator 不可用时返回 `VALIDATOR_UNAVAILABLE` error、`complete:false` 并拒绝提交；正式 CLI 会装载目标图 runtime，同步 seam 在缺少 runtime 时 fail-closed。
- Workflow 0.1 无私有 data 的 relation 简写可无损读取，并返回 `LEGACY_SCHEMA_ADAPTED` notice 与 `complete:false`。
- MCP 与 Server 切图会异步装配目标图 runtime；Server 返回 diagnostics、complete、notice，Web 复用公开字段白名单吸收元数据。
- 空图初始化经 Core bootstrap 做基础校验，并使用 recoverable journal、history 与 audit 持久化。

最终规格轴 blocker：0。

## 惯例轴

- Core 仍是唯一图存储写入方；模块只提供由 Core 调用的领域操作实现，并以 `MutationPlan` 请求图变更。
- 发布包不含 Workflow、Research、Exploration 等领域模块源码，只含 Core 产品、Web 资产、Module SDK、三宿主投影和四个基础 Skills。
- Product Identity 仍由 `package.json` 派生，Codex、Claude、Pi 保持独立 manifest、entrypoint 与 wiring。
- 最后一个 Web 修复复用了现有 `updateMetadata` 白名单，没有新增第二套过滤协议。

最终惯例轴 blocker：0。

## 独立验证与裁决

- `npm run verify`：根测试 27 files / 105 tests；Web 11 files / 47 tests；Svelte typecheck 0 error / 0 warning；package smoke 123 entries，全部通过。
- `git diff --check`：通过。
- 图校验：26 nodes / 40 edges，0 error / 0 warning。
- 独立复核最终返回 `awaiting_adjudication`；协调者依据两轴证据裁决复核通过。

候选提交隔离和 tarball 身份由 `l4_release_candidate.candidate` checkpoint 单独记录。
