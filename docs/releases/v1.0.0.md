# 1.0.0 发布证据材料

状态：**代码就绪，待属主发布**（2026-09-23）。本文是发布门证据与发布动作清单；
`npm publish` 与 `git push` 刻意未执行，属 M5 边界（蓝本 §9 发布 = 用户动作）。

## 一、发布门证据（2026-09-23 实测）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 平台类型 | 根 `npm run typecheck`（9 包 tsc --noEmit） | 全绿 |
| 平台测试（= 单一门禁，blueprint §8） | 根 `npm test`（vitest：packages 后端缝 + web-ui jsdom） | **28 文件 / 208 用例全绿** |
| workflow 模块发布门 | toporealm-workflow 仓库根 `npm run verify`（typecheck + vitest） | **10 文件 / 45 用例全绿** |

workflow 模块发布门构成（0.x 语义测试的 1.0 形态，语义零损失口径）：

- 纯领域：domain（七态/三档/三来源/三关系、流转矩阵、payload 往返）、evidence（聚合/完成门禁/代签）、
  adjudication（independent 建议不伪造 human）、scheduler（frontier/blocked/汇合/missing/stale）。
- 真缝（ModuleHost + DaemonCore + 模块 dist 直激活）：12 命令注册与目录投影（D24② forms 在案）、
  happy path 全链路、human 代签拒绝、P07 认领竞争、retry 预算、fallback 前置、DANGLING_RELATION。
- 钩子执法：CLI 直改 VETOED（流转/依赖/完成/代签/checkpoint 五类）、同状态幂等放行、
  **D24① undo/redo 豁免回归**（passed 的合法逆转可撤销可重做）、undo 后截断 redo 段。
- program dogfood：entry→exit 覆盖并行、汇合、重试、fallback、iteration、human、自验、建议裁决；
  统一提交日志（D7）>40 条。
- 打包与 skills：npm pack --ignore-scripts 自包含形状（无 dependencies、无安装脚本）；六 skills 审计。

集成验收（toporealm 仓库 `packages/cli/test/workflow-module.e2e.test.ts`，在根 `npm test` 内）：
真实 workflow 模块**本地 path 安装** → daemon 装载 → `cmds` 可见 12 条 `wf.*` →
`wf.create-task` / `wf.transition-task` / `wf.claim-task` / `wf.next-actions` 端到端 →
running→passed 无证据被钩子 `VETOED` → `undo` 回退认领。

## 二、版本对齐

- 根 `1.0.0-alpha.1 → 1.0.0`；10 包（protocol / module-sdk / daemon-core / module-host /
  client / daemon / web / cli / distribution / web-ui）同步 `1.0.0`；跨包依赖精确锁 `1.0.0`。
- toporealm-workflow：包版本 = module.yaml 版本 = `1.0.0`（module.yaml v2，ns `wf`）。
- 发布形态补齐（浏览器人工验收后、发布前）：新增聚合包 `@lukawi/toporealm`
  （blueprint §2 发布物，bin: toporealm + toporeald）；cli/daemon 增 `tsx` 运行时依赖、
  cli 增 daemon 依赖——TS 源直发形态下 bin 开箱可用。

## 三、发布动作清单（属主执行；本轮未执行）

1. **toporealm**：
   ```bash
   npm test                      # 复跑门禁（应 28 文件 / 208 用例全绿）
   git tag v1.0.0 && git push && git push --tags
   cd packages/protocol && npm publish        # 依赖序：protocol → 其余包
   # 随后按依赖序发布：module-sdk、daemon-core、module-host、client、web、daemon、distribution、cli
   # web-ui 视属主意图决定是否发布（当前 private 与否以包内 publishConfig 为准）
   ```
2. **toporealm-workflow**：
   ```bash
   npm run verify                # 复跑发布门（应 10 文件 / 45 用例全绿）
   git tag v1.0.0 && git push && git push --tags
   npm publish                   # publishConfig.access=public
   ```
3. GitHub Release：两侧各建 `v1.0.0`，正文以本文件第一节 + 各自 CHANGELOG 为底稿。
4. 发布后 smoke：`npm install @lukawi/toporealm`（或 npx）→ `toporealm version` →
   `toporealm module add @lukawi/toporealm-workflow` → `toporealm cmds` 可见 `wf.*`。

## 四、已知边界与遗留（如实）

- **TS source 直发**：各包 `main/exports` 指向 `src/*.ts`（alpha 起的既定形态）。
  消费方需 TS 运行支持；cli/daemon bin 已内置 tsx 加载，装完即用。
- **web-ui 真实浏览器人工验收**：✅ 已于发布前（2026-09-23）用内置浏览器执行并通过——
  页面加载与图渲染、CLI 提交无刷新实时同步（r8→r9）、页面撤销/重做真缝回路（r10/r11，磁盘逆变更正确）、
  外部手改 YAML 吸收与 reset 自愈提示（r12）、开页静置 40s daemon 存活并实时跟进（D22④ 回归，pid 不变）、
  节点检查器（详情/关系/payload）。截图表面在自动化环境超时，以 DOM 证据为准。
- **workflow 模块测试的平台来源**：`file:` devDependencies 指向同级 `toporealm` monorepo；
  平台包已发布 npm，后续可切回 registry 版本号（后续里程碑接线）。
- **pi 宿主受管目录布局**：M5 起 `.pi/skills/` 整目录为单一所有权标记；alpha 期旧投影
  （`.pi/skills/toporealm/.toporealm-sync.json`）会在下次 `host sync` 后成为孤儿标记文件，无害。

## 五、发布执行记录（2026-09-23）

- tag `v1.0.0` + push：toporealm（887d5e4）与 toporealm-workflow（7fd3bcc）。
- npm 发布：`@lukawi/toporealm`（聚合）+ 9 个子包 `1.0.0`（protocol → module-sdk → daemon-core →
  module-host → client → web → daemon → distribution → cli）；web-ui 为 private 不发布。
- GitHub Release：两仓库各建 `v1.0.0`。
- 发布后 smoke（干净目录、registry 真装）：聚合包与 cli 两种入口均验证——`version` / `new` /
  `module add @lukawi/toporealm-workflow`（registry 来源落位 + 绑定）/ `cmds` 12 条 `wf.*` /
  `wf.create-task → transition ready → claim running`（门禁 VETOED 与 hint 自恢复路径实测）/ `next-actions` / `status`。
