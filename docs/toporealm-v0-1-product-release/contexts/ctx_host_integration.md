# Agent 与宿主集成（ctx_host_integration）

负责 toporealm、toporealm-design、toporealm-join、toporealm-grilling 四个基础 Skills 及 Codex、Claude、Pi 的直接可识别投影；不负责领域执行纪律和模块业务 Skills。

## 术语表

- **意图对齐**: Agent 通过事实自查和逐项用户决策消除需求歧义，形成可供设计与 ADR 使用的共识。 Avoid: 自动替用户决策

> 本文由 `graph export` 从 context 顶点 ctx_host_integration 生成（节点即文档，图是真相源）。
