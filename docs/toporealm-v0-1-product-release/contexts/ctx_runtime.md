# 基座运行时（ctx_runtime）

负责领域无关的图存储、工作区与图解析、CLI、stdio MCP、Server 和 Web UI 运行时；不负责领域模块实现与发布平台运维。

## 术语表

- **基座产品**: 基座及其 CLI、MCP、Web UI、基础 Skills 和宿主集成组成的独立产品；不包含领域模块。 Avoid: 内置模块套装

> 本文由 `graph export` 从 context 顶点 ctx_runtime 生成（节点即文档，图是真相源）。
