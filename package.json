{
  "name": "toporealm-monorepo",
  "private": true,
  "version": "1.0.0-alpha.1",
  "description": "TopoRealm 1.0 — single-owner daemon graph foundation (monorepo)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LUKAWI/toporealm.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "web-ui"
  ],
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "typecheck": "tsc -p packages/protocol --noEmit && tsc -p packages/module-sdk --noEmit && tsc -p packages/daemon-core --noEmit && tsc -p packages/module-host --noEmit && tsc -p packages/web --noEmit && tsc -p packages/client --noEmit && tsc -p packages/daemon --noEmit && tsc -p packages/distribution --noEmit && tsc -p packages/cli --noEmit",
    "test": "vitest run",
    "demo": "node --import tsx scripts/demo.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
