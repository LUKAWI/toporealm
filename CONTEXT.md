# TopoRealm 统一术语

## 基座（Base）

提供图的持久化、引用、查询、演化和通用交互环境，但不规定特定领域如何解释节点、边或完成状态的稳定平台。

## 图（Graph）

一组具有稳定身份的对象及其显式关系。图可以同时承载多个模块的对象，也不要求整体具有统一生命周期或最终完成状态。

## 对象（Object）

图中可被独立引用的节点。对象的主类型表达“它是什么”，装载的能力表达“它能做什么”。

## 关系（Relation）

两个对象之间有类型、有方向或无方向的显式联系。关系是否参与排序、推荐或约束，由定义它的模块决定。

## 主类型（Kind）

对象的领域身份。一个对象只有一个主类型，主类型使用模块命名空间避免歧义。

## 能力（Capability）

可以组合到对象或关系上的状态、行为和规则。能力不改变承载者的主类型。

## 主类型数据（Kind Data）

由对象或关系的主类型所属模块拥有并解释的领域信息。主类型模块缺失时，主类型数据仍保持原有语义结构。

## 能力状态（Capability State）

由某项能力拥有并解释、随对象或关系保存的数据。能力状态使用带模块命名空间的能力身份归属。

## 模块（Module）

可装载的领域能力包，声明自己的对象类型、关系类型、字段、规则、操作、推荐策略、视图和导出方式。

## 模块组合（Module Composition）

同一张图同时装载多个模块，使不同领域的对象能够建立关系并各自遵循所属模块的规则。

## 基座 Skill（Base Skill）

随 TopoRealm 基座提供、且不依赖特定领域模块的 agent 使用方法。基座 Skill 可以识别图与模块、指导通用建图，但不替领域模块决定对象的业务内容或生命周期。

## 图入场（Graph Onboarding）

新会话以只读方式了解一张图的范围、已启用模块、主要对象、近期变化和当前关注区域，再把后续工作交给适用能力的过程。图入场不改变对象状态，也不开始领域执行。

## 模块 Skill（Module Skill）

由模块提供、教 agent 使用该模块领域能力的方法。模块未安装时其 Skill 不存在；模块已安装但当前图未启用时，其 Skill 不在该图中运行。

## 模块安装（Module Installation）

使模块的运行时、界面贡献和 Skills 对当前 TopoRealm 环境可用。

## 图级启用（Graph Activation）

声明某张图使用一个已安装模块。它决定模块能力和 Skills 能否作用于该图，不改变模块在其他图中的可用性。

## 完整校验（Full Validation）

基座结构校验与所有已声明模块的领域校验均可执行且通过。缺少模块时，只能完成基础校验，不能声称完成完整校验。

## 未知区域（Unknown Region）

尚未形成稳定认识的开放问题或范围。未知区域具有稳定身份，可以并存、拆分、解决和重新打开。

## 发行组合（Distribution）

基座、若干模块、默认配置与使用方法的成套产品。Super Plumber 将作为面向开发交付的发行组合延续。
