# TopoRealm 统一术语（1.0）

> 与 1.0 蓝图（`docs/rebuild/blueprint.md`）配套的现行词汇表。0.x 术语（注册快照、变更计划、可恢复提交、能力、fail-closed 校验等）随契约废弃，不再收录。

## 基座产品（Base Product）

可独立安装的本地图工作空间产品：守护进程 + CLI + WebUI + 模块系统 + 宿主适配。基座产品不包含领域模块。

## 图（Graph）

一组具有稳定身份的对象及其显式关系。一张图可同时承载多个模块的对象，不要求统一生命周期或完成状态。

## 图清单（Graph Manifest）

声明图的身份、revision、undo 游标与所启用模块的入口记录。不枚举单个对象和关系。

## 对象（Object）

图中可被独立引用的节点。主类型表达"它是什么"，其余信息都在载荷中。

## 关系（Relation）

两个对象之间有类型、有方向的显式联系，与对象同为图的一等实体。关系是否参与排序、推荐或约束，由定义它的模块决定。

## 实体 ID（Entity ID）

对象或关系在所属图内不可变且共同唯一的身份。名称变化不改变实体 ID。

## 主类型（Kind）

对象的领域身份。一个对象只有一个主类型；主类型使用模块命名空间避免歧义，无命名空间前缀即公共/无主类型。

## 模块命名空间（Module Namespace）

模块在图中用于标识其主类型与归属的稳定前缀。一张图不能同时装载两个使用相同命名空间的模块。

## 载荷（Payload）

对象或关系上不透明的数据区域，core 原样存取、永不解释。其内部结构由拥有该主类型的模块约定。

## 守护进程（Daemon）

独占一张图、持有内存态并以原子写落盘的常驻本地进程。所有读写的唯一执行者、图文件的唯一写者。

## 客户端（Client）

守护进程之外的薄接入面：CLI 与 Web UI。客户端不直接读写图文件。

## 提交（Commit）

一组变更作为原子单位生效的唯一方式。一次提交 = 一个 undo 单位。

## 变更（Change）

CLI、WebUI 与模块共用的写词汇：put（写入对象）、rel（写入关系）、merge（载荷浅合并）、del（删除）。三个缝学一次，处处使用。

## 提交日志（Commit Log）

每图一份 append-only 记录，记载每次提交的变更、来源、label 与时间。兼任撤销栈、近期变更源与审计。

## 所有权法（Ownership Rule）

core 仅有的两条执法之一：模块来源的提交只能触碰自己命名空间下的主类型或公共/无主主类型。它是模块间互信的协调边界（防手滑），不是安全边界（防恶意靠安装时刻的信任）；人类提交豁免——人是图的最终属主。

## 悬空边检查（Dangling Relation Check）

core 仅有的两条执法之二：关系的端点必须存在。

## 校验钩子（Validation Hook）

模块订阅的 before-commit 事件：检查候选变更（含变更前后双快照）、附加诊断、可否决。core 不聚合、不排序、不 fail-closed。领域校验的唯一标准形态。

## 模块（Module）

可装载的领域能力包，以双层形态存在。

## 双层模块（Two-layer Module）

模块的标准形态：声明层（清单：身份、命名空间、依赖、主类型词汇、UI 投影、入口）负责协调——注册、发现、防冲突；代码层（`activate` 时注册命令、钩子、表单）负责行为，拥有全权 API。声明层不是执法依据；信任在安装时刻完成。

## 命令（Command）

模块注册的、人和 agent 都可调用的操作。元数据在代码注册时声明，由守护进程自省提供目录，目录永远等于注册事实。

## 模块组合（Module Composition）

同一张图同时装载多个模块，使不同领域的对象能够建立关系。跨模块互操作的正道是图数据面；模块集在守护进程启动时冻结。

## 模块绑定（Module Binding）／工作区模块／模块分发载体

工作区对模块来源与版本的选择；由单个工作区保存或链接的模块；npm 包等传递载体（载体不改变模块的身份或权限）。

## 图级启用（Graph Activation）

图清单声明其使用的已安装模块，决定模块能力与 Skill 能否作用于该图。

## 基座 Skill／模块 Skill

随基座提供、不依赖领域模块的 agent 使用方法；由模块提供、教授其领域能力的方法。模块未安装时其 Skill 不存在。

## 宿主投影（Host Projection）

为特定 agent 宿主（Claude Code、Pi）生成的 skills 与配置资产，由 host sync 按所有权标记管理。

## 图入场（Graph Onboarding）

新会话以只读方式了解一张图的范围、已启用模块与近期变化（主要数据源即提交日志），再把后续工作交给适用能力的过程。不改变对象状态。
