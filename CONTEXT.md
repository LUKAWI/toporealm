# TopoRealm 统一术语（1.0 / 1.1.0 规划已收口）

> 与蓝图（`docs/rebuild/blueprint.md`，1.1.0 裁决见其 §1.8）配套的现行词汇表。0.x 术语（注册快照、变更计划、可恢复提交、能力、fail-closed 校验等）随契约废弃，不再收录。

## 基座产品（Base Product）

可独立安装的本地图工作空间产品：守护进程 + CLI + WebUI + 模块系统 + 宿主适配。基座产品不包含领域模块。

## 图（Graph）

一组具有稳定身份的对象及其显式关系。一张图可同时承载多个模块的对象，不要求统一生命周期或完成状态。

## 图清单（Graph Manifest）

声明图的身份、revision 与 undo 游标的入口记录。不枚举单个对象、关系或模块。

## 对象（Object）

图中可被独立引用的节点。主类型表达"它是什么"，其余信息都在载荷中。

## 关系（Relation）

两个对象之间有类型、有方向的显式联系，与对象同为图的一等实体。关系是否参与排序、推荐或约束，由定义它的模块决定。

## 实体 ID（Entity ID）

对象或关系在所属图内不可变且共同唯一的身份。名称变化不改变实体 ID。

## 主类型（Kind）

对象的领域身份。一个对象只有一个主类型；主类型使用模块命名空间避免歧义，无命名空间前缀即公共/无主类型。

## 模块命名空间（Module Namespace）

模块在图中用于标识其主类型与归属的稳定前缀。一张图不能同时装载两个使用相同命名空间的模块。

## 载荷（Payload）

对象或关系上不透明的数据区域，core 原样存取、永不解释。其内部结构由拥有该主类型的模块约定。

## 守护进程（Daemon）

独占一张图、持有内存态并以原子写落盘的常驻本地进程。所有读写的唯一执行者、图文件的唯一写者。

## 客户端（Client）

守护进程之外的薄接入面：CLI 与 Web UI。客户端不直接读写图文件。

## 提交（Commit）

一组变更作为原子单位生效的唯一方式。一次提交 = 一个 undo 单位。

## 变更（Change）

CLI、WebUI 与模块共用的写词汇：put（写入对象）、rel（写入关系）、merge（载荷浅合并）、del（删除）。三个缝学一次，处处使用。

## 提交日志（Commit Log）

每图一份 append-only 记录，记载每次提交的变更、来源、label 与时间。兼任撤销栈、近期变更源与审计。

## 所有权法（Ownership Rule）

core 仅有的两条执法之一：模块来源的提交只能触碰自己命名空间下的主类型或公共/无主主类型。它是模块间互信的协调边界（防手滑），不是安全边界（防恶意靠安装时刻的信任）；人类提交豁免——人是图的最终属主。

## 悬空边检查（Dangling Relation Check）

core 仅有的两条执法之二：关系的端点必须存在。

## 校验钩子（Validation Hook）

模块订阅的 before-commit 事件：检查候选变更（含变更前后双快照）、附加诊断、可否决。core 不聚合、不排序、不 fail-closed。领域校验的唯一标准形态。

## 模块（Module）

可装载的领域能力包，以双层形态存在。

## 双层模块（Two-layer Module）

模块的标准形态：声明层（清单：身份、命名空间、依赖、主类型词汇、UI 投影、入口）负责协调——注册、发现、防冲突；代码层（`activate` 时注册命令、钩子、表单）负责行为，拥有全权 API。声明层不是执法依据；信任在安装时刻完成。

## 命令（Command）

模块注册的、人和 agent 都可调用的操作。元数据在代码注册时声明，由守护进程自省提供目录，目录永远等于注册事实。

## 模块组合（Module Composition）

同一张图同时装载多个模块，使不同领域的对象能够建立关系。跨模块互操作的正道是图数据面；模块集在守护进程启动时冻结。

## 作用域（Scope）

模块生效的范围，由安装位置决定：全局池装的模块对所有项目生效，项目池装的仅本项目生效。装了就生效，没有第三步启用动作；项目装同 id 模块时项目遮蔽全局。

## 全局池（Global Pool）

用户目录下、对所有项目共享的模块存储。目录即注册；模块技能文件随模块留在池中。

## 模块链接（Module Link）

以 path 来源指向工作区外本地模块目录的声明（`modules.yaml` 的唯一职责）。模块作者开发期与测试接模块的工作方式；不是安装，不产生所有权标记。

## 图级启用

（1.1.0 废除）1.0 的概念：图清单声明其使用的已安装模块。实现中本就是工作区级全量装载，该分层从未真正按图生效；由「作用域」取代。

## 基座 Skill／模块 Skill

随基座提供、不依赖领域模块的 agent 使用方法；由模块提供、教授其领域能力的方法。模块未安装时其 Skill 不存在。

## 宿主适配（Host Integration）

面向特定 agent 宿主（Claude Code、Pi）的接入方式：基座技能与入场钩子由基座发布物静态携带（claude 走 marketplace 插件，pi 走包内置扩展）；toporealm 不向宿主自有目录写入任何文件。

## 技能索引（Skills Index）

基座在 agent 会话启动时提供的可用模块技能清单：技能名、所属模块、一句话描述、技能文件路径。claude 侧经 SessionStart 钩子注入上下文，agent 按需读取技能全文；pi 侧经扩展把技能目录贡献给宿主原生发现。技能文件始终只存在于池中，索引不复制内容。

## 基座 Skill／模块 Skill

随基座提供、不依赖领域模块的 agent 使用方法（教授 CLI 用法）；由模块提供、教授其领域能力的方法。模块未安装时其 Skill 不存在；模块 Skill 随模块进池，经技能索引到达 agent。

## 专注图（Focused Graph）

工作区当前被选定编辑的图：CLI 编辑的缺省目标、WebUI 的实时编辑对象。由 `use` 选定，同一时刻只有一张；切换是全局动作，WebUI 自动跟随。

## 静态预览（Static Preview）

非专注图在 WebUI 中的只读呈现：一份即时快照。不实时同步、不可编辑、无撤销。

## 图入场（Graph Onboarding）

新会话以只读方式了解一张图的范围、当前作用域内的模块与近期变化（主要数据源即提交日志），再把后续工作交给适用能力的过程。不改变对象状态。
