{
  "name": "@lukawi/toporealm-distribution",
  "version": "1.1.0",
  "description": "TopoRealm 1.0 distribution: module installer (npm pack --ignore-scripts + local path, ownership marker), host sync (claude-code plugin / pi extension+skills), 0.x migrate. Cold-path only: called by the cli, never touches a running daemon.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "@lukawi/toporealm-daemon-core": "1.1.0",
    "@lukawi/toporealm-protocol": "1.1.0",
    "yaml": "^2.8.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
