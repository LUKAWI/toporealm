---
name: example
description: Create example.card objects only when the current TopoRealm graph enables the example fixture module.
---

Discover `example.create-card`, then call it with an id and label. The runtime returns a `MutationPlan`; it does not write graph storage directly.
